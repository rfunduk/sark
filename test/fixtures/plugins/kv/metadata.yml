title: KV
description: Smoke-test plugin. A single key-value table.

tables:
  kv:
    description: Arbitrary text key/value pairs with updated-at tracking.
    columns:
      key:        Primary key. Free-form text identifier.
      value:      Stored value. Free-form text.
      updated_at: ISO-8601 UTC timestamp, set on insert via DEFAULT.
