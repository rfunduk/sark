# Sark config. Copy to `config.yml` and edit. `config.yml` is gitignored.
#
# Run with:
#   mix sark --config config.yml
# or, in a release:
#   sark --config /etc/sark/config.yml

listen: 127.0.0.1:8080
data_dir: ./dev_data
log_level: info
hot_reload: true

# Bearer tokens. Each entry:
#   { name: <operator label>, plugins: [<plugin names>] | ["*"], token: <secret> }
#
# `plugins` scopes the token to specific plugins by name. Use `["*"]` for
# all plugins. Logs reference `name`, never `token`. Generate with:
#   openssl rand -hex 32
# Use ${VAR} to pull from env:
#   - { name: laptop, plugins: ["*"], token: "${SARK_LAPTOP_TOKEN}" }
tokens:
  - { name: dev, plugins: ["*"], token: sk-CHANGEME }

# Plugin map: name → directory. The name is the routing key everywhere
# (URL path `/<name>/mcp`, DB filename, token scopes), so it stays
# stable even if you rename the on-disk directory. Paths are absolute
# or relative to this config file.
plugins: {}
